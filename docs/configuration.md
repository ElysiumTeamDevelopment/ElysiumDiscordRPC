# Конфигурация ElysiumDiscordRPC

Подробное руководство по настройке файла `discord_rpc_config.rpy` - центрального места всех настроек модуля.

## 📋 Обзор конфигурации

Файл `discord_rpc_config.rpy` содержит все настройки Discord RPC модуля, разделённые на логические секции:

- 🔧 **Обязательные настройки** - Application ID и название игры
- 🖼️ **Ресурсы изображений** - большие и маленькие изображения
- 📱 **Шаблоны статусов** - готовые шаблоны для разных состояний
- ⚙️ **Технические настройки** - подключение, очереди, логирование
- 🤖 **Автоматическое отслеживание** - паттерны лейблов и персонажей

## 🔧 Обязательные настройки

### Application ID
```python
define discord_config.application_id = "123456789012345678"
```

**Как получить:**
1. Перейдите на [Discord Developer Portal](https://discord.com/developers/applications)
2. Создайте новое приложение или выберите существующее
3. Скопируйте **Application ID** из раздела "General Information"

**Важно:** Без корректного Application ID модуль не будет работать!

### Название игры
```python
define discord_config.game_name = "Моя Крутая Игра"
```

**Опционально:** Если не указано, будет использовано `config.name` из `options.rpy`.

## 🖼️ Ресурсы изображений

### Загрузка изображений в Discord

**Перед настройкой изображений:**
1. Перейдите в Discord Developer Portal → ваше приложение
2. Откройте раздел "Rich Presence" → "Art Assets"
3. Загрузите изображения (рекомендуемый размер: 512x512px)
4. Запомните **имена** загруженных изображений

### Большие изображения
```python
define discord_config.large_images = {
    "game_icon": "main_logo",           # Основная иконка игры
    "menu": "menu_background",          # Фон главного меню
    "gameplay": "gameplay_scene",       # Игровой процесс
    "chapter1": "forest_background",    # Фон 1 главы
    "chapter2": "city_background",      # Фон 2 главы
    "ending": "ending_scene",           # Концовка
}
```

### Маленькие изображения
```python
define discord_config.small_images = {
    "playing": "play_button",           # Индикатор игры
    "paused": "pause_button",           # Индикатор паузы
    "menu": "menu_icon",                # Индикатор меню
    "reading": "book_icon",             # Чтение диалога
    "choice": "question_mark",          # Выбор опции
}
```

**Принцип работы:**
- Ключ (например, `"game_icon"`) используется в коде
- Значение (например, `"main_logo"`) - имя изображения в Discord

## 📱 Шаблоны статусов

### Главное меню
```python
define discord_config.main_menu_presence = {
    "state": "В главном меню",
    "details": None,                    # Автоматически подставится game_name
    "large_image": "game_icon",
    "large_text": None,                 # Автоматически подставится game_name
    "small_image": "menu",
    "small_text": "Главное меню"
}
```

### Игровой процесс
```python
define discord_config.gameplay_presence = {
    "state": "Играет",
    "details": None,                    # Автоматически подставится game_name
    "large_image": "gameplay",
    "large_text": None,
    "small_image": "playing",
    "small_text": "В игре"
}
```

### Пауза
```python
define discord_config.paused_presence = {
    "state": "На паузе",
    "details": None,
    "large_image": "gameplay",
    "small_image": "paused",
    "small_text": "Пауза"
}
```

### Чтение диалога
```python
define discord_config.dialogue_presence = {
    "state": "Читает диалог",
    "details": None,
    "large_image": "gameplay",
    "small_image": "reading",
    "small_text": "Диалог"
}
```

**Автоматическое заполнение:**
- `None` в `details` и `large_text` автоматически заменяется на `game_name`
- Это позволяет легко изменить название игры в одном месте

## ⚙️ Технические настройки

### Подключение
```python
define discord_config.connection = {
    "startup_sync_enabled": True,       # Синхронное подключение при старте
    "startup_timeout": 5.0,             # Максимум секунд ожидания при старте
    "max_retries": 3,                   # Максимум попыток переподключения
    "retry_delay": 5.0,                 # Секунд между попытками
    "connection_timeout": 30.0,         # Максимум секунд для подключения
    "update_timeout": 10.0,             # Максимум секунд для обновления
    "health_check_interval": 60.0,      # Секунд между проверками здоровья
}
```

**Рекомендации:**
- `startup_sync_enabled: True` - для стабильного подключения
- `startup_sync_enabled: False` - для мгновенного запуска игры

### Очереди
```python
define discord_config.queue = {
    "max_pending_updates": 10,          # Максимум обновлений в очереди при подключении
    "max_reliability_queue": 100,       # Максимум в очереди reliability модуля
}
```

### Логирование
```python
define discord_config.logging = {
    "log_connections": True,            # Логировать события подключения
    "log_updates": True,                # Логировать обновления статуса
    "log_errors": True,                 # Логировать ошибки
    "log_status_changes": True,         # Логировать смены статуса
    "log_reliability": False,           # Логировать события reliability (подробно)
}
```

## 🤖 Автоматическое отслеживание

### Основные настройки
```python
define discord_config.auto_tracking = {
    "enabled": True,                    # Включить автоотслеживание
    "track_labels": True,               # Отслеживать смену лейблов
    "track_characters": True,           # Отслеживать диалоги персонажей
    "track_menus": True,                # Отслеживать навигацию по меню
}
```

### Паттерны лейблов
```python
define discord_config.label_patterns = {
    "chapter_": "Глава {chapter}",      # chapter_1 → "Глава 1"
    "scene_": "Сцена {scene}",          # scene_park → "Сцена park"
    "menu_": "Меню {menu}",             # menu_settings → "Меню settings"
    "ending_": "Концовка {ending}",     # ending_good → "Концовка good"
    "route_": "Путь {route}",           # route_alice → "Путь alice"
}
```

**Как работает:**
1. При переходе на лейбл `chapter_1` автоматически устанавливается статус "Глава 1"
2. Часть после `_` форматируется (заменяются `_` на пробелы, первая буква заглавная)

### Имена персонажей
```python
define discord_config.character_names = {
    "e": "Эйлин",                       # Объект персонажа 'e' → "Эйлин"
    "narrator": "Рассказчик",           # narrator → "Рассказчик"
    "alice": "Алиса",
    "bob": "Боб",
    "villain": "Антагонист",
}
```

**Использование:**
```python
# В script.rpy:
define alice = Character("Алиса")

# В discord_rpc_config.rpy:
define discord_config.character_names = {
    "alice": "Алиса"
}

# Результат: при диалоге alice автоматически показывается "Разговор с Алиса"
```

## 🔘 Кнопки Discord

```python
define discord_config.buttons = [
    {
        "label": "Играть в игру",
        "url": "https://example.com/play"
    },
    {
        "label": "Сайт разработчика", 
        "url": "https://example.com"
    }
]
```

**Ограничения:**
- Максимум 2 кнопки
- Кнопки не работают в собственном профиле (тестируйте на других аккаунтах)

## 🎯 Примеры конфигурации

### Минимальная конфигурация
```python
# Только самое необходимое
define discord_config.application_id = "123456789012345678"
define discord_config.game_name = "Моя VN"
```

### Базовая конфигурация
```python
define discord_config.application_id = "123456789012345678"
define discord_config.game_name = "Эпическая История"

define discord_config.large_images = {
    "game_icon": "main_logo"
}

define discord_config.character_names = {
    "protagonist": "Главный герой",
    "love_interest": "Возлюбленная"
}
```

### Продвинутая конфигурация
```python
define discord_config.application_id = "123456789012345678"
define discord_config.game_name = "Эпическая История: Путь Героя"

# Полный набор изображений
define discord_config.large_images = {
    "game_icon": "logo_512",
    "chapter1": "forest_bg",
    "chapter2": "city_bg", 
    "chapter3": "castle_bg",
    "ending_good": "sunset_bg",
    "ending_bad": "storm_bg"
}

define discord_config.small_images = {
    "playing": "sword_icon",
    "paused": "pause_icon",
    "choice": "crossroads_icon",
    "reading": "scroll_icon"
}

# Детальные паттерны
define discord_config.label_patterns = {
    "ch": "Глава {ch}",
    "scene": "{scene}",
    "route": "Путь {route}",
    "battle": "Битва: {battle}",
    "ending": "Концовка: {ending}"
}

# Все персонажи
define discord_config.character_names = {
    "hero": "Главный герой",
    "princess": "Принцесса Элара",
    "wizard": "Мудрец Гэндальф",
    "villain": "Тёмный Лорд",
    "narrator": "Рассказчик"
}

# Кастомные кнопки
define discord_config.buttons = [
    {
        "label": "Скачать игру",
        "url": "https://mygame.com/download"
    },
    {
        "label": "Наш Discord",
        "url": "https://discord.gg/mygame"
    }
]

# Тонкая настройка подключения
define discord_config.connection = {
    "startup_sync_enabled": True,
    "startup_timeout": 3.0,             # Быстрее для SSD
    "max_retries": 5,                   # Больше попыток
    "retry_delay": 3.0,                 # Быстрее переподключение
}
```

## ✅ Валидация конфигурации

Модуль автоматически проверяет конфигурацию при запуске:

### Успешная валидация
```
✅ Discord RPC Configuration is valid
```

### Ошибки конфигурации
```
Discord RPC Configuration ERRORS:
  ❌ discord_config.application_id must be set to your Discord Application ID

Discord RPC Configuration WARNINGS:
  ⚠️ discord_config.application_id should be a 17-19 digit number
  ⚠️ discord_config.large_images['game_icon'] should be set
```

## 🛠️ Вспомогательные функции

### Получение настроек
```python
# Получить значение из конфига с fallback
timeout = get_discord_config('connection.startup_timeout', 5.0)
max_retries = get_discord_config('connection.max_retries', 3)
```

### Работа с шаблонами
```python
# Получить готовый шаблон статуса
presence = get_presence_template('main_menu_presence')
discord_rpc.update_presence(**presence)
```

### Работа с ресурсами
```python
# Получить имя ресурса изображения
large_img = resolve_image_asset('game_icon', 'large')    # → "main_logo"
small_img = resolve_image_asset('playing', 'small')     # → "sword_icon"
```

### Форматирование
```python
# Автоматическое форматирование имён
formatted = format_label_name('chapter_1')              # → "Глава 1"
char_name = get_character_display_name(hero)            # → "Главный герой"
```

## 🔄 Обновление конфигурации

После изменения конфигурации:

1. **Сохраните файл** `discord_rpc_config.rpy`
2. **Перезапустите игру** (Shift+R в RenPy)
3. **Проверьте логи** на наличие ошибок валидации
4. **Протестируйте** Discord RPC в игре

## 💡 Советы по конфигурации

1. **Начните с минимума** - только `application_id` и `game_name`
2. **Постепенно добавляйте** изображения и функции
3. **Тестируйте каждое изменение** перед добавлением следующего
4. **Используйте осмысленные имена** для ключей изображений
5. **Документируйте изменения** комментариями в конфиге
6. **Создайте резервную копию** рабочей конфигурации

## 🔗 Связанные разделы

- **[API Reference](api-reference.md)** - использование настроек в коде
- **[Examples](examples.md)** - примеры с различными конфигурациями
- **[Troubleshooting](troubleshooting.md)** - решение проблем с конфигурацией
