# Установка ElysiumDiscordRPC

Подробное руководство по установке и настройке модуля Discord Rich Presence для RenPy.

## 📋 Требования

### Системные требования
- **RenPy:** 7.4+ (рекомендуется 8.0+)
- **Python:** 3.9+ (встроен в RenPy 8.0+)
- **Discord:** Установленный Discord клиент
- **Операционная система:** Windows, macOS, Linux

### Подготовка Discord приложения
1. Перейдите на [Discord Developer Portal](https://discord.com/developers/applications)
2. Нажмите "New Application"
3. Введите название вашей игры
4. Скопируйте **Application ID** (понадобится для конфигурации)
5. (Опционально) Загрузите изображения в раздел "Rich Presence" → "Art Assets"

## 📦 Варианты установки

### Вариант 1: Core Package (минимум)

**Для кого:** Разработчики с кастомным интерфейсом

**Файлы для копирования:**
```
ElysiumDiscordRPC/
├── discord_rpc_config.rpy      # Конфигурация
├── discord_rpc.rpy             # Основной модуль
├── discord_rpc_api.rpy         # API функции
└── python-packages/
    └── pypresence/             # Библиотека Discord RPC
```

### Вариант 2: Full Package (все возможности)

**Для кого:** Пользователи, желающие готовое решение

**Файлы для копирования:**
```
ElysiumDiscordRPC/
├── discord_rpc_config.rpy      # Конфигурация
├── discord_rpc.rpy             # Основной модуль
├── discord_rpc_api.rpy         # API функции
├── discord_rpc_settings.rpy    # Встроенный UI
├── discord_rpc_reliability.rpy # Дополнительная надёжность
└── python-packages/
    └── pypresence/             # Библиотека Discord RPC
```

## 🗂️ Размещение файлов

### Рекомендуемое размещение

**В директории `game/` (стандартно):**
```
your_renpy_project/
├── game/
│   ├── discord_rpc_config.rpy
│   ├── discord_rpc.rpy
│   ├── discord_rpc_api.rpy
│   ├── discord_rpc_settings.rpy    # опционально
│   ├── discord_rpc_reliability.rpy # опционально
│   ├── python-packages/
│   │   └── pypresence/
│   ├── script.rpy                  # ваши файлы
│   ├── options.rpy
│   └── screens.rpy
├── README.md
└── project.json
```

### Альтернативные варианты размещения

**В отдельной директории модулей:**
```
your_renpy_project/
├── game/
│   ├── modules/
│   │   ├── discord_rpc/
│   │   │   ├── discord_rpc_config.rpy
│   │   │   ├── discord_rpc.rpy
│   │   │   └── discord_rpc_api.rpy
│   │   └── python-packages/
│   │       └── pypresence/
│   ├── script.rpy
│   └── options.rpy
```

**В корне проекта (для больших проектов):**
```
your_renpy_project/
├── discord_rpc/
│   ├── discord_rpc_config.rpy
│   ├── discord_rpc.rpy
│   └── discord_rpc_api.rpy
├── game/
│   ├── python-packages/
│   │   └── pypresence/
│   ├── script.rpy
│   └── options.rpy
```

> **Важно:** Библиотека `pypresence` должна быть в `game/python-packages/` независимо от размещения модуля.

## 🔧 Пошаговая установка

### Шаг 1: Скачивание модуля

**Через Git:**
```bash
git clone https://github.com/ElysiumDevelopment/ElysiumDiscordRPC.git
cd ElysiumDiscordRPC
```

**Через GitHub:**
1. Перейдите на страницу релизов
2. Скачайте `ElysiumDiscordRPC-core.zip` или `ElysiumDiscordRPC-full.zip`
3. Распакуйте архив

### Шаг 2: Копирование файлов

1. Скопируйте выбранные файлы модуля в ваш RenPy проект
2. Убедитесь, что структура директорий соответствует выбранному варианту

### Шаг 3: Установка pypresence

**Через uv (рекомендуется):**
```bash
cd your_renpy_project
uv pip install pypresence --target game/python-packages
```

**Через pip:**
```bash
cd your_renpy_project
pip install pypresence --target game/python-packages
```

**Ручная установка:**
1. Скачайте pypresence с [PyPI](https://pypi.org/project/pypresence/)
2. Распакуйте в `game/python-packages/pypresence/`

### Шаг 4: Базовая конфигурация

Отредактируйте `discord_rpc_config.rpy`:

```python
# ОБЯЗАТЕЛЬНО: Ваш Discord Application ID
define discord_config.application_id = "123456789012345678"

# РЕКОМЕНДУЕМО: Название игры
define discord_config.game_name = "Моя Крутая Игра"

# ОПЦИОНАЛЬНО: Основные изображения
define discord_config.large_images = {
    "game_icon": "main_logo",           # Загрузите в Discord Developer Portal
    "gameplay": "gameplay_icon",
}
```

### Шаг 5: Проверка установки

1. Запустите RenPy проект
2. Проверьте консоль на наличие ошибок
3. Должно появиться сообщение: `✅ Discord RPC Configuration is valid`
4. В Discord должен отобразиться статус игры

## 🔍 Проверка корректности установки

### Проверка файлов
```python
# В консоли RenPy или в игре:
$ print("Discord RPC available:", 'discord_rpc' in globals())
$ print("Config valid:", hasattr(discord_config, 'application_id'))
$ print("API available:", 'discord_set_custom' in globals())
```

### Проверка подключения
```python
# В игре:
$ print("Status:", discord_rpc.get_status())
$ print("Connected:", discord_rpc.connected)
$ print("Enabled:", discord_rpc.enabled)
```

### Тестовое обновление статуса
```python
# В игре:
$ discord_set_custom("Тестирование", "Проверка установки")
```

## ⚠️ Возможные проблемы

### "pypresence not available"
**Решение:**
1. Убедитесь, что pypresence установлен в `game/python-packages/`
2. Проверьте структуру: `game/python-packages/pypresence/__init__.py` должен существовать
3. Переустановите pypresence

### "Discord RPC Configuration ERRORS"
**Решение:**
1. Проверьте, что `discord_config.application_id` установлен
2. Убедитесь, что Application ID корректный (17-19 цифр)
3. Проверьте синтаксис в `discord_rpc_config.rpy`

### "Discord not found"
**Решение:**
1. Запустите Discord клиент
2. Убедитесь, что вы авторизованы в Discord
3. Проверьте, что Discord не заблокирован антивирусом

### Игра не запускается
**Решение:**
1. Проверьте синтаксис всех .rpy файлов
2. Убедитесь, что все файлы скопированы корректно
3. Проверьте логи RenPy на наличие ошибок

## 🎯 Следующие шаги

После успешной установки:

1. **[Настройте конфигурацию](configuration.md)** - детальная настройка модуля
2. **[Изучите API](api-reference.md)** - доступные функции
3. **[Посмотрите примеры](examples.md)** - готовые примеры кода
4. **[Интегрируйте в UI](ui-integration.md)** - добавление в интерфейс игры

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте [Troubleshooting](troubleshooting.md)
2. Создайте [Issue на GitHub](https://github.com/ElysiumDevelopment/ElysiumDiscordRPC/issues)
3. Приложите логи RenPy и описание проблемы
