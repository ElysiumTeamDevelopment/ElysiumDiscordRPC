# Troubleshooting

Руководство по решению проблем с ElysiumDiscordRPC.

## 🚨 Частые проблемы

### "pypresence not available"

**Симптомы:**
```
Warning: pypresence not available. Discord RPC will be disabled.
```

**Причины и решения:**

1. **Библиотека не установлена**
   ```bash
   # Установите pypresence
   cd your_renpy_project
   uv pip install pypresence --target game/python-packages
   ```

2. **Неправильная структура директорий**
   ```
   # Проверьте структуру:
   game/python-packages/pypresence/__init__.py  # Должен существовать
   ```

3. **Права доступа (Linux/macOS)**
   ```bash
   chmod -R 755 game/python-packages/pypresence/
   ```

### "Discord RPC Configuration ERRORS"

**Симптомы:**
```
Discord RPC Configuration ERRORS:
  ❌ discord_config.application_id must be set to your Discord Application ID
```

**Решения:**

1. **Установите Application ID**
   ```python
   # В discord_rpc_config.rpy
   define discord_config.application_id = "123456789012345678"
   ```

2. **Проверьте формат ID**
   - Должен быть строкой из 17-19 цифр
   - Без пробелов и специальных символов
   - Получите на https://discord.com/developers/applications

3. **Проверьте синтаксис**
   ```python
   # Правильно:
   define discord_config.application_id = "123456789012345678"
   
   # Неправильно:
   define discord_config.application_id = 123456789012345678  # Без кавычек
   ```

### "Discord not found" / "No Discord client"

**Симптомы:**
- Статус: "Ошибка"
- Сообщение о том, что Discord не найден

**Решения:**

1. **Запустите Discord**
   - Убедитесь, что Discord клиент запущен
   - Войдите в свой аккаунт

2. **Проверьте процессы**
   ```bash
   # Windows
   tasklist | findstr Discord
   
   # Linux/macOS
   ps aux | grep -i discord
   ```

3. **Перезапустите Discord**
   - Полностью закройте Discord (включая системный трей)
   - Запустите заново

4. **Проверьте антивирус**
   - Добавьте Discord в исключения
   - Временно отключите антивирус для теста

### Игра не запускается после установки модуля

**Симптомы:**
- RenPy выдаёт ошибки при запуске
- Игра зависает на загрузке

**Решения:**

1. **Проверьте синтаксис .rpy файлов**
   ```python
   # Запустите RenPy в режиме разработчика
   # Проверьте логи на ошибки синтаксиса
   ```

2. **Проверьте структуру файлов**
   ```
   game/
   ├── discord_rpc_config.rpy      # Обязательно
   ├── discord_rpc.rpy             # Обязательно
   ├── discord_rpc_api.rpy         # Обязательно
   └── python-packages/
       └── pypresence/             # Обязательно
   ```

3. **Временно отключите модуль**
   ```python
   # Переименуйте файлы для отключения
   discord_rpc_config.rpy.disabled
   discord_rpc.rpy.disabled
   discord_rpc_api.rpy.disabled
   ```

4. **Проверьте логи RenPy**
   - Откройте консоль разработчика (Shift+O)
   - Проверьте файл log.txt в директории игры

## ⚠️ Проблемы подключения

### Статус "Подключение" не меняется

**Симптомы:**
- Статус застрял на "Подключение"
- Нет обновлений статуса в Discord

**Решения:**

1. **Проверьте таймауты**
   ```python
   # В discord_rpc_config.rpy увеличьте таймауты
   define discord_config.connection = {
       "startup_timeout": 10.0,        # Увеличить с 5.0
       "connection_timeout": 60.0,     # Увеличить с 30.0
   }
   ```

2. **Отключите синхронизацию при запуске**
   ```python
   define discord_config.connection = {
       "startup_sync_enabled": False,  # Асинхронное подключение
   }
   ```

3. **Переподключитесь вручную**
   ```python
   $ discord_rpc.disconnect()
   $ discord_rpc.connect()
   ```

### Статус "Ошибка" постоянно

**Симптомы:**
- Статус не меняется с "Ошибка"
- Переподключение не помогает

**Решения:**

1. **Проверьте Application ID**
   ```python
   # Убедитесь, что ID корректный
   $ print(discord_rpc.client_id)
   ```

2. **Создайте новое Discord приложение**
   - Перейдите на https://discord.com/developers/applications
   - Создайте новое приложение
   - Используйте новый Application ID

3. **Проверьте сетевое подключение**
   - Убедитесь, что интернет работает
   - Проверьте файрвол и прокси

4. **Сбросьте настройки**
   ```python
   $ discord_rpc.retry_count = 0
   $ discord_rpc.last_error = None
   $ discord_rpc.connect()
   ```

## 🔧 Проблемы конфигурации

### Изображения не отображаются

**Симптомы:**
- Статус обновляется, но изображения не показываются
- Показываются стандартные изображения Discord

**Решения:**

1. **Загрузите изображения в Discord**
   - Перейдите в Discord Developer Portal
   - Откройте ваше приложение → Rich Presence → Art Assets
   - Загрузите изображения (рекомендуемый размер: 512x512px)

2. **Проверьте имена изображений**
   ```python
   # В discord_rpc_config.rpy
   define discord_config.large_images = {
       "game_icon": "exact_name_in_discord",  # Точное имя из Discord
   }
   ```

3. **Подождите обновления**
   - Изображения могут обновляться до 15 минут
   - Перезапустите Discord для ускорения

### Автоматическое отслеживание не работает

**Симптомы:**
- Статус не обновляется при смене лейблов
- Персонажи не отслеживаются

**Решения:**

1. **Проверьте настройки автоотслеживания**
   ```python
   # В discord_rpc_config.rpy
   define discord_config.auto_tracking = {
       "enabled": True,                # Должно быть True
       "track_labels": True,
       "track_characters": True,
   }
   ```

2. **Проверьте паттерны лейблов**
   ```python
   # Убедитесь, что паттерны соответствуют вашим лейблам
   define discord_config.label_patterns = {
       "chapter_": "Глава {chapter}",  # Для лейблов chapter_1, chapter_2
   }
   ```

3. **Проверьте имена персонажей**
   ```python
   # Добавьте ваших персонажей
   define discord_config.character_names = {
       "e": "Эйлин",                   # Для Character("Эйлин", name="e")
   }
   ```

## 🐛 Проблемы производительности

### Игра тормозит после установки модуля

**Симптомы:**
- Снижение FPS
- Задержки при переходах между сценами

**Решения:**

1. **Отключите подробное логирование**
   ```python
   # В discord_rpc_config.rpy
   define discord_config.logging = {
       "log_connections": False,
       "log_updates": False,
       "log_reliability": False,
   }
   ```

2. **Увеличьте интервалы обновления**
   ```python
   define discord_config.connection = {
       "health_check_interval": 120.0,  # Увеличить с 60.0
   }
   ```

3. **Отключите reliability модуль**
   - Удалите или переименуйте `discord_rpc_reliability.rpy`

### Частые переподключения

**Симптомы:**
- Статус часто меняется между "Подключен" и "Подключение"
- Уведомления о переподключении

**Решения:**

1. **Увеличьте задержки**
   ```python
   define discord_config.connection = {
       "retry_delay": 10.0,            # Увеличить с 5.0
       "max_retries": 2,               # Уменьшить с 3
   }
   ```

2. **Проверьте стабильность Discord**
   - Обновите Discord до последней версии
   - Перезапустите Discord

## 📱 Проблемы на разных платформах

### Linux: Проблемы с правами доступа

**Решения:**
```bash
# Установите правильные права
chmod -R 755 game/python-packages/
chmod +x your_game_executable

# Проверьте зависимости
ldd your_game_executable | grep -i discord
```

### macOS: Проблемы с безопасностью

**Решения:**
```bash
# Разрешите выполнение
xattr -dr com.apple.quarantine your_game.app

# Проверьте права в System Preferences → Security & Privacy
```

### Windows: Проблемы с антивирусом

**Решения:**
1. Добавьте игру в исключения антивируса
2. Добавьте Discord в исключения
3. Временно отключите Real-time protection для теста

## 🔍 Диагностика

### Проверка состояния модуля

```python
# В консоли RenPy (Shift+O) или в игре:

# Проверка доступности
$ print("Discord RPC available:", 'discord_rpc' in globals())
$ print("pypresence available:", PYPRESENCE_AVAILABLE)

# Проверка конфигурации
$ print("Application ID:", discord_config.application_id)
$ print("Game name:", get_game_name())

# Проверка состояния
$ print("Enabled:", discord_rpc.enabled)
$ print("Connected:", discord_rpc.connected)
$ print("Status:", discord_rpc.get_status())

# Проверка ошибок
$ info = discord_rpc.get_status_info()
$ print("Last error:", info['last_error'])
$ print("Retry count:", info['retry_count'])
```

### Тестирование функций

```python
# Тест базовых функций
$ discord_set_custom("Тест", "Проверка работы")
$ discord_set_main_menu()
$ discord_clear()

# Тест подключения
$ discord_rpc.disconnect()
$ discord_rpc.connect()

# Тест конфигурации
$ template = get_presence_template('main_menu_presence')
$ print("Template:", template)
```

### Включение отладочного режима

```python
# В discord_rpc_config.rpy
define discord_config.logging = {
    "log_connections": True,
    "log_updates": True,
    "log_errors": True,
    "log_status_changes": True,
    "log_reliability": True,        # Подробные логи
}

# Проверяйте консоль RenPy на сообщения
```

## 📞 Получение помощи

### Информация для отчёта об ошибке

При создании issue на GitHub включите:

1. **Версия RenPy:** `renpy.version()`
2. **Операционная система:** Windows/macOS/Linux + версия
3. **Версия Discord:** Настройки → Информация о приложении
4. **Конфигурация модуля:** содержимое `discord_rpc_config.rpy`
5. **Логи ошибок:** из консоли RenPy
6. **Шаги воспроизведения:** что делали когда возникла ошибка

### Полезные команды для диагностики

```python
# Полная диагностика
$ print("=== Discord RPC Diagnostic ===")
$ print("RenPy version:", renpy.version())
$ print("pypresence available:", PYPRESENCE_AVAILABLE)
$ print("Config valid:", hasattr(discord_config, 'application_id'))
$ print("Module enabled:", discord_rpc.enabled)
$ print("Connection status:", discord_rpc.get_status())
$ info = discord_rpc.get_status_info()
$ for key, value in info.items():
$     print(f"{key}: {value}")
```

## 🔗 Связанные разделы

- **[Installation](installation.md)** - проблемы установки
- **[Configuration](configuration.md)** - проблемы конфигурации
- **[API Reference](api-reference.md)** - правильное использование функций
